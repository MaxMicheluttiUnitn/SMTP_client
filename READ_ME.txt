Please install TKinker for python as follows (use sudo if needed):

apt-get install python3-tk

And remember to download the required libraries:

pip install -r requirements.txt

To succesfullly send gmail mails remember create and fill the .env with your email and Google App-Password